<%= form_for(@boul) do |f| %>
  <% if @boul.errors.any? %>
    <div id="error_explanation">
      <h2 class="red">Il semble qu'il y ait un petit problème...</h2>
      <ul>
      <% @boul.errors.messages.each do |msg| %>
        <li><strong><%= msg[1][0] %></strong></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
